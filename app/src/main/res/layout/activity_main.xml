<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0B0F19">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="⚡ Auto Equalizer"
            android:textColor="#F9FAFB"
            android:textSize="22sp"
            android:textStyle="bold"
            android:layout_marginBottom="12dp" />

        <!-- Main EQ card, styled after a hardware/plugin EQ panel -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/card_background"
            android:padding="16dp">

            <!-- Header row: preset name (left) + enable toggle (right) -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:layout_marginBottom="16dp">

                <Spinner
                    android:id="@+id/presetSpinner"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1" />

                <com.google.android.material.switchmaterial.SwitchMaterial
                    android:id="@+id/serviceToggle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content" />
            </LinearLayout>

            <!-- Frequency labels row -->
            <LinearLayout
                android:id="@+id/freqLabelsRow"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal" />

            <!-- Vertical slider bank -->
            <LinearLayout
                android:id="@+id/slidersRow"
                android:layout_width="match_parent"
                android:layout_height="260dp"
                android:orientation="horizontal"
                android:layout_marginTop="4dp" />

            <!-- Live dB value labels row -->
            <LinearLayout
                android:id="@+id/valueLabelsRow"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginTop="4dp" />

        </LinearLayout>

        <TextView
            android:id="@+id/statusText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Service stopped"
            android:textColor="#9CA3AF"
            android:textSize="14sp" />

        <Button
            android:id="@+id/permissionButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="Grant Audio Permission" />

        <com.google.android.material.switchmaterial.SwitchMaterial
            android:id="@+id/autoModeToggle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:checked="true"
            android:text="Auto mode (analyze &amp; adjust automatically — sliders show manual override)"
            android:textColor="#F9FAFB" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:text="How the auto curve works: live spectrum analysis + a fixed equal-loudness compensation curve (bass/treble are perceived quieter than mids at equal physical level) + your chosen preset offset, smoothed with attack/release so it doesn't jitter on every beat."
            android:textColor="#6B7280"
            android:textSize="11sp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="Note: works on the mix session Android exposes to apps (session 0). Coverage varies by device and by app — some apps (exclusive/offloaded audio, some DRM playback) can't be reached this way. This is the same technique used by third-party system EQ apps; it is not guaranteed on every device."
            android:textColor="#6B7280"
            android:textSize="11sp" />

    </LinearLayout>
</ScrollView>
